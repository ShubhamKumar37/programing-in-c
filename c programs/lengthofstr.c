#include<stdio.h>
int main(){
    char Str[1000];
    int i;

    printf("enter the string");
    scanf("%s",Str);
    for (i=0; Str[i]!='\0'; ++i){
    }
    printf("length of string %d",i);
        
    return 0;
}